import {
  beforeEachProviders,
  describe,
  ddescribe,
  expect,
  iit,
  it,
  inject,
  injectAsync,
  ComponentFixture,
  TestComponentBuilder
} from 'angular2/testing';
import {provide} from 'angular2/core';
import {CustomValidatedFormComponent} from './custom-validated-form.component';

describe('CustomValidatedForm Component', () => {

  beforeEachProviders((): any[] => []);

  it('should ...', injectAsync([TestComponentBuilder], (tcb:TestComponentBuilder) => {
    return tcb.createAsync(CustomValidatedFormComponent).then((fixture: ComponentFixture) => {
      fixture.detectChanges();
    });
  }));

});
